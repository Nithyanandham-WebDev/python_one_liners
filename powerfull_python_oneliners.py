# palindrome python one-liner

phrase.find(phrase[::-1])


# swap two variable python one-linear

a,b = b,a


# sum over every other value python one-linear

sum (stock_prices[::2])

# read over every value python one-linear

[line.strip() for line in open(filename)]

# factorial python one-linear

reduce(lambda x,y :x*y,range(1,n+1))

